starterkit:
  name:
    type: strings
    is_required: TRUE
  description:
    type: strings
    is_required: TRUE
  modules:
    type: arrays
    is_required: FALSE
    next_level_validation: TRUE
    require:
      type: arrays
      is_required: FALSE
    install:
      type: arrays
      is_required: FALSE
  themes:
    type: arrays
    is_required: FALSE
    next_level_validation: TRUE
    require:
      type: arrays
      is_required: FALSE
    install:
      type: arrays
      is_required: FALSE
    admin:
      type: strings
      is_required: FALSE
    default:
      type: strings
      is_required: FALSE
